# Default values for ocr.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ankurgos/ocr
  tag: test
  pullPolicy: Always

mongouser: root
mongopwd: password
mongourl: cosmos-mongodb.default.svc.cluster.local
mongoport: 27017

